sudo emerge "sys-process/htop"

sudo emerge "sys-apps/dstat"

sudo emerge "net-analyzer/nethogs"

sudo emerge "app-admin/pydf"

sudo emerge "sys-power/powertop"

sudo emerge "app-admin/lnav"
sudo lnav -i extra
lnav -i extra

sudo emerge iotop
sudo emerge smartmontools
